version: '3.8'

services:
  # --- INFRA ---
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  db:
    image: timescale/timescaledb:latest-pg14
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=llminfra
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  # --- SERVICES (RUST) ---
  orchestrator:
    build:
      context: .
      dockerfile: Dockerfile.rust
      args:
        SERVICE_NAME: inventiv-orchestrator
    command: cargo watch -x "run --release -p inventiv-orchestrator"
    ports:
      - "8001:8001"
    volumes:
      - .:/app # Source code mount for hot reload
      - cargo_cache:/usr/local/cargo/registry # Cache dependencies
      - target_cache:/app/target # Cache compiled artifacts
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/llminfra
      - REDIS_URL=redis://redis:6379
      - PROVIDER=scaleway
      - SCALEWAY_PROJECT_ID=${SCALEWAY_PROJECT_ID}
      - SCALEWAY_ACCESS_KEY=${SCALEWAY_ACCESS_KEY}
      - SCALEWAY_SECRET_KEY=${SCALEWAY_SECRET_KEY}
      - RUST_LOG=debug
    depends_on:
      - db
      - redis

  api:
    build:
      context: .
      dockerfile: Dockerfile.rust
      args:
        SERVICE_NAME: inventiv-api
    command: cargo watch -x "run --release -p inventiv-api"
    ports:
      - "8003:8003"
    volumes:
      - .:/app # Source code mount for hot reload
      - cargo_cache:/usr/local/cargo/registry # Cache dependencies
      - target_cache:/app/target # Cache compiled artifacts
    environment:
      - REDIS_URL=redis://redis:6379
      - DATABASE_URL=postgresql://postgres:password@db:5432/llminfra
      - RUST_LOG=debug
    depends_on:
      - redis
      - db

volumes:
  redis_data:
  db_data:
  cargo_cache: # NEW: Cache Cargo dependencies
  target_cache: # NEW: Cache compilation artifacts
