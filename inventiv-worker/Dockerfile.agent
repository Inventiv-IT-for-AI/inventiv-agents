FROM python:3.11-slim

WORKDIR /app

COPY agent.py .

RUN pip install --no-cache-dir requests

ENV CONTROL_PLANE_URL=""
ENV WORKER_AUTH_TOKEN=""
ENV INSTANCE_ID=""
ENV WORKER_ID=""
ENV MODEL_ID="demo-model"
ENV VLLM_BASE_URL="http://mock-vllm:8000"
ENV WORKER_HEALTH_PORT="8080"
ENV WORKER_VLLM_PORT="8000"
ENV WORKER_HEARTBEAT_INTERVAL_S="5"

CMD ["python", "agent.py"]

